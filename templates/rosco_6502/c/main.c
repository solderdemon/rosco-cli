#include <stdio.h>

int main(void) {
  printf("Hello, world! 😃\n");

  // When this function returns, the machine goes back to the firmware
  // monitor. In a real program, you probably wouldn't return from main.
  return 0;
}
